# Traces

Raw traces are excluded by `.gitignore` under `traces/raw/`.

Public-safe redacted traces should live under `traces/redacted/` and preserve node sequence, tool sequence, evaluator outcomes, grader outcomes, risk band, latency, and estimated cost metadata.
